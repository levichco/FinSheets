# Univer_Levich Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-06-29

## Active Technologies

- TypeScript 5.x (ESM), targeting React 18.3+ / 19 host apps + `@univerjs/presets` + `@univerjs/preset-sheets-core` (pinned EXACT, e.g. `0.25.0`); `exceljs` (full-fidelity `.xlsx` write); build via `tsup` (001-univer-levich-sheets)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.x (ESM), targeting React 18.3+ / 19 host apps: Follow standard conventions

## Recent Changes

- 001-univer-levich-sheets: Added TypeScript 5.x (ESM), targeting React 18.3+ / 19 host apps + `@univerjs/presets` + `@univerjs/preset-sheets-core` (pinned EXACT, e.g. `0.25.0`); `exceljs` (full-fidelity `.xlsx` write); build via `tsup`

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
